<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.qsq.auth.mapper.AuthServerMapper">


    <select id="getUserRoleByUserId" resultType="java.lang.String">
            select  sr.role_name as role
            from sys_user_role sur LEFT JOIN sys_role sr on sr.role_id = sur.role_id
            where sur.user_id = #{id}
    </select>

</mapper>
